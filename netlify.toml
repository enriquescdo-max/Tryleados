[build]
  base    = "frontend"
  command = "npm install && npm run build"
  publish = "dist"

[build.environment]
  NODE_VERSION = "20"
  VITE_API_URL = "https://tryleados-production.up.railway.app"

[[redirects]]
  from   = "/*"
  to     = "/index.html"
  status = 200
